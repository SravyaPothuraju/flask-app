apiVersion: v1
kind: Service
metadata:
  name: flask-app-service 
  labels:
    app: flask-app
spec:
  selector:
    app: flask-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3000
  type: ClusterIP

